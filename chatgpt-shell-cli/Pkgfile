# Description: Simple shell script to use OpenAI's ChatGPT and DALL-E from the terminal.
# URL: https://github.com/0xacx/chatGPT-shell-cli
# Maintainer: deadfysh, deadfysh at protonmail dot com
# Depends on: curl jq

name=chatgpt-shell-cli
version=git
release=1
source=()

build() {
	git clone https://github.com/0xacx/chatGPT-shell-cli.git $name
    cd $name

	chmod +x chatgpt.sh
    install -Dm755 chatgpt.sh "$PKG/usr/bin/chatgpt"
}
